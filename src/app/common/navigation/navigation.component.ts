// import * as angular from 'angular';

const NavigationComponent: ng.IComponentOptions = {
    template:
        `
            <a ui-sref="todos">Todos</a>
        `
};

export default NavigationComponent;
