import * as angular from 'angular';

const HeaderComponent: ng.IComponentOptions = {
    template:
        `
            <h1>Web Build Management Playground</h1>
        `
};

export default HeaderComponent;
